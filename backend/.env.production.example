# ===========================================
# LinkUp Golf Backend - Production Environment Variables
# ===========================================
# For Railway: Add these in Railway Dashboard > Variables
# For Render: Add these in Render Dashboard > Environment
# For Heroku: heroku config:set KEY=value

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=production
PORT=3001
LOG_LEVEL=info

# ===========================================
# Demo Mode
# ===========================================
# Set to "true" for demo deployment (bypasses Clerk auth)
# Set to "false" for production with real authentication
DEMO_MODE=true

# ===========================================
# Database (Neon PostgreSQL)
# ===========================================
# Get your connection string from https://neon.tech
# Use the POOLED connection string for production
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:your_password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Connection pool settings (for high-traffic)
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# ===========================================
# Clerk Authentication
# ===========================================
# Get your keys from https://clerk.com/dashboard
# For DEMO_MODE=true, use placeholder values
CLERK_SECRET_KEY=sk_live_your_clerk_secret_key
CLERK_PUBLISHABLE_KEY=pk_live_your_clerk_publishable_key
CLERK_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# ===========================================
# CORS Configuration
# ===========================================
# Your frontend Vercel URL (no trailing slash)
CORS_ORIGIN=https://your-app.vercel.app

# For multiple origins (comma-separated)
# CORS_ORIGIN=https://your-app.vercel.app,https://www.yourdomain.com

# ===========================================
# Redis (Optional - for caching/rate limiting)
# ===========================================
# Get free Redis from https://upstash.com
# Format: rediss://default:password@host:port
REDIS_URL=rediss://default:your_password@us1-xxx.upstash.io:6379

# ===========================================
# Rate Limiting
# ===========================================
RATE_LIMIT_READ_PER_MIN=100
RATE_LIMIT_WRITE_PER_MIN=30

# ===========================================
# Weather API (Optional)
# ===========================================
# Get free API key from https://openweathermap.org/api
# 1000 calls/day on free tier
OPENWEATHER_API_KEY=your_openweather_api_key

# ===========================================
# Security
# ===========================================
# JWT secret for internal tokens (if used)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Trusted proxy settings
TRUST_PROXY=true

# ===========================================
# Monitoring (Optional)
# ===========================================
# Sentry error tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx

# ===========================================
# Notes
# ===========================================
# 1. Never commit real credentials to version control
# 2. Use Railway/Render's secret management for sensitive values
# 3. DATABASE_URL must use ?sslmode=require for Neon
# 4. CORS_ORIGIN must match your frontend URL exactly
