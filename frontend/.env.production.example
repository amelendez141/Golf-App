# ===========================================
# LinkUp Golf Frontend - Production Environment Variables
# ===========================================
# Copy this file to .env.production.local for local production testing
# For Vercel deployment, add these in the Vercel Dashboard > Settings > Environment Variables

# ===========================================
# Required: API Configuration
# ===========================================
# Backend API URL (Railway/Render deployment URL)
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app

# WebSocket URL for real-time features
NEXT_PUBLIC_WS_URL=wss://your-realtime-server.up.railway.app

# ===========================================
# Required: Clerk Authentication
# ===========================================
# Get these from https://clerk.com/dashboard
# For DEMO mode, these can be placeholder values
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your_publishable_key
CLERK_SECRET_KEY=sk_live_your_secret_key

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/feed
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/feed

# ===========================================
# Optional: Mapbox - Interactive Golf Course Maps
# ===========================================
# The app works without Mapbox - it shows a friendly setup guide instead.
# When configured, users can explore courses on an interactive map.
#
# HOW TO GET YOUR FREE MAPBOX TOKEN:
# 1. Visit https://account.mapbox.com/
# 2. Create a free account (no credit card required)
# 3. Find "Default public token" on your dashboard
# 4. Copy the token (starts with "pk.")
#
# FREE TIER INCLUDES:
# - 50,000 map loads per month (plenty for most apps)
# - All map styles (outdoors, satellite, etc.)
# - No credit card required
#
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token

# ===========================================
# Optional: Analytics & Monitoring
# ===========================================
# Vercel Analytics (automatically added if enabled in Vercel)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# Sentry (error tracking)
# SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_DSN=

# ===========================================
# Build Configuration
# ===========================================
# Disable telemetry in production builds
NEXT_TELEMETRY_DISABLED=1

# ===========================================
# Demo Mode Note
# ===========================================
# If your backend has DEMO_MODE=true, you don't need real Clerk keys
# The app will work with demo users and bypass authentication
