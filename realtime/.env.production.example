# ===========================================
# LinkUp Golf Realtime Server - Production Environment Variables
# ===========================================
# For Railway: Add these in Railway Dashboard > Variables
# For Render: Add these in Render Dashboard > Environment

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=production
WS_PORT=3002
LOG_LEVEL=info

# ===========================================
# Database (Neon PostgreSQL)
# ===========================================
# Same database as backend - use pooled connection
DATABASE_URL=postgresql://neondb_owner:your_password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# ===========================================
# Redis (Required for BullMQ job queues)
# ===========================================
# Get free Redis from https://upstash.com
# BullMQ requires Redis for job processing
REDIS_URL=rediss://default:your_password@us1-xxx.upstash.io:6379

# ===========================================
# JWT Authentication
# ===========================================
# Must match the backend JWT_SECRET for token validation
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# ===========================================
# Web Push Notifications (Optional)
# ===========================================
# Generate keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_SUBJECT=mailto:notifications@linkupgolf.com

# ===========================================
# Email Notifications (Optional)
# ===========================================
# SMTP settings for email notifications
# For Gmail: Use App Password, not regular password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="LinkUp Golf <notifications@linkupgolf.com>"

# ===========================================
# CORS Configuration
# ===========================================
# Frontend URLs allowed to connect via WebSocket
ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.yourdomain.com

# ===========================================
# Job Queue Settings
# ===========================================
# How often to run scheduled jobs (in ms)
JOB_POLL_INTERVAL=60000

# Maximum concurrent jobs
JOB_CONCURRENCY=5

# ===========================================
# Notes
# ===========================================
# 1. WebSocket connections require always-on hosting (no sleep)
# 2. Railway free tier doesn't sleep if there's active connections
# 3. Render requires paid plan ($7/mo) for WebSocket support
# 4. Redis is required for BullMQ job processing
# 5. Web Push and Email are optional features
